---
ansible-navigator:
  execution-environment:
    container-engine: podman
    enabled: true
    image: quay.io/deversmarht/compliance_ee:2.0
    pull:
      policy: missing
  playbook-artifact:
    enable: False
  logging:
    level: critical
  mode: stdout