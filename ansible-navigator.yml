---
ansible-navigator:
  execution-environment:
    container-engine: podman
    enabled: true
    image: quay.io/jerrycorum/homelab_ee:1.13
    pull:
      policy: missing
  playbook-artifact:
    enable: False
  logging:
    level: critical
  mode: stdout