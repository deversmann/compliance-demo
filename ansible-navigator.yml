---
ansible-navigator:
  execution-environment:
    container-engine: podman
    enabled: true
    image: quay.io/jerrycorum/compliance_ee:1.0
    pull:
      policy: missing
  playbook-artifact:
    enable: False
  logging:
    level: critical
  mode: stdout